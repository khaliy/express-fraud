{
  "name": "express-fraud",
  "description": "Node.js Fraud detection middleware for Express.js",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "khaliy/express-fraud",
  "dependencies": {
    "lodash": "^4.2.0"
  },
  "devDependencies": {
    "istanbul": "0.4.2",
    "mocha": "2.4.5",
    "supertest": "1.1.0"
  },
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "mocha --check-leaks --reporter spec --bail",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --check-leaks --reporter dot",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --check-leaks --reporter spec"
  }
}
